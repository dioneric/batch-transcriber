{
  "name": "batchtranscriber",
  "version": "1.0.0",
  "description": "Transcribes mono wav files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx nexe index.js -o transcribe -t windows --build --verbose"
  },
  "author": "Eric Dion",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/speech": "^4.5.6",
    "@google-cloud/storage": "^5.12.0"
  },
  "devDependencies": {
    "nexe": "^4.0.0-beta.18"
  }
}
